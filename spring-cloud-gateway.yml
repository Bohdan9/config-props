spring-config-server:
  permissions:
    VIEW_BANK_PROFILE:
      - GET:/bank-profile/
    EDIT_BANK_PROFILE:
      - PUT:/bank-profile/
  routes:
    bank-service:
      - GET:/bank-profile/**
      - PUT:/bank-profile/**
